const chai = require('chai');
const expect = chai.expect;

var Game = require('../src/Game');
var Player = require('../src/Player');

describe('Game', () => {
  let game;
  it.skip('should accept an array of Player class instances', () => {
    game = new Game();
  })
})